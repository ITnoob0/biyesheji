阶段一：成果审批流与版本记录（核心管理闭环）
核心逻辑区分： 成果由“学院管理员”负责审核，他们只能看到本学院的待审核成果；“系统管理员”具有全局查看权限或最终仲裁权。

Agent 提示词：

Plaintext

```
你现在是我的全栈高级开发工程师。请帮我在现有的科研管理系统中实现“成果审批流与版本管理”功能。
当前系统的管理员分为“系统管理员(System Admin)”和“学院管理员(College Admin)”。请按照以下步骤开发，确保代码鲁棒性且严格遵守角色权限边界：

1. **后端模型更新 (`backend/achievements/models.py`)**：
   - 为 `Paper` 等主要成果模型添加 `status` 字段（枚举类型：DRAFT, PENDING_REVIEW, APPROVED, REJECTED），默认值为 DRAFT。
   - 补充 `AchievementOperationLog` 模型，用于记录字段级别的变更历史（如：“级别从C类改为B类”）、操作人、操作时间以及驳回原因等审核意见。

2. **后端 API 与权限扩展 (`backend/achievements/views.py` & `serializers.py`)**：
   - 更新 Serializer 以包含 `status` 字段。
   - 实现审核接口 `/api/achievements/papers/<id>/approve/` 和 `/api/achievements/papers/<id>/reject/`。
   - **权限与数据隔离（重要）**：
     - 当角色为“学院管理员”时，待审核列表接口只返回该管理员所属学院教师提交的 `PENDING_REVIEW` 成果。
     - 驳回操作必须必填 `reason` 参数，并写入 Log 模型。
   - 教师侧限制：如果状态是 `APPROVED`，教师编辑后状态自动重置为 `PENDING_REVIEW`，或只能走“修改申请”流程。

3. **前端 UI 更新 (`frontend/src/views/achievement-entry/`)**：
   - 教师端：成果列表根据 `status` 增加不同颜色的状态标签。
   - 学院管理员端：在管理面板（如 `PaperGovernancePanel.vue`）增加“本院待审核” Tab 页，提供“通过”和“驳回（弹窗填写理由）”按钮。
   - 增加“审核与变更时间轴”抽屉组件，前端调用日志接口，清晰展示该成果从草稿到最终过审的所有流转节点。
     阶段二：成果认领与消歧（解决数据孤岛）
     核心逻辑区分： 认领主要发生在教师之间，但学院管理员需要在后台能看到“有哪些成果长期处于未认领状态”以便催办。
```

Agent 提示词：

Plaintext

```
你现在是我的全栈高级开发工程师。请帮我在系统中实现“校内合著者成果认领机制”。
目的是解决多位本校教师合作发表论文时的重复录入问题。

1. **后端模型设计 (`backend/achievements/models.py`)**：
   - 创建 `AchievementClaim` 模型，字段：`achievement_id` (关联成果)、`initiator_id` (录入者)、`target_user_id` (被邀请认领的本校教师)、`status` (PENDING, ACCEPTED, REJECTED)、`created_at`。

2. **后端自动识别逻辑 (`backend/achievements/services.py`)**：
   - 拦截教师成功录入/导入论文后的钩子函数：解析合著者列表。
   - 根据姓名匹配规则，在系统中查找同名的其他有效教师（排除录入者本人）。
   - 为匹配到的本校教师在 `AchievementClaim` 表自动生成 `PENDING` 状态的认领邀请。

3. **前端交互与管理侧呈现 (`frontend/src/views/`)**：
   - **教师端**：个人中心增加显著的“待认领成果”徽标（Badge）。点击后进入认领列表，展示论文摘要，提供“确认认领（加入我的画像统计）”和“不是我（拒绝）”操作。
   - **学院管理员端**：在学院看板或成果治理面板中，增加一个“异常/未认领数据追踪”区块，允许学院管理员查看本院长期未被认领的成果，并提供“一键发送系统提醒”的功能。
     阶段三：教师画像的同侪对比与快照（升华分析价值）
     核心逻辑区分： 教师的对比基准应该是“本学院平均”，系统管理员则可以查看“全校平均”与“各学院平均”的对比。
```

Agent 提示词：

Plaintext

```
你现在是我的全栈高级开发工程师。请帮我在“教师画像”模块中增强数据分析能力，引入“同侪基准对比”和“历史年份快照”功能。注意区分学院管理员和系统管理员的视角。

1. **历史快照模型 (`backend/achievements/portrait_analysis.py` & `models.py`)**：
   - 创建 `PortraitSnapshot` 模型固化历史数据：`user`、`year`、`dimension_scores` (JSON)、`created_at`。
   - 提供按年份查询画像的 API。

2. **同侪基准计算逻辑 (`backend/achievements/portrait_analysis.py`)**：
   - 增加按维度聚合计算的逻辑。
   - **数据分层**：计算出 `college_average_data` (该教师所在学院的平均分) 和 `university_average_data` (全校平均分)。
   - API 根据请求用户的角色返回不同的对比参照物：教师默认获取同院平均，系统管理员可获取全校平均。

3. **前端雷达图深度渲染 (`frontend/src/components/RadarChart.vue` & `PortraitDeepAnalysisPanel.vue`)**：
   - 升级 ECharts 雷达图配置，支持多图层叠合渲染。
   - **教师视角**：图例包含“我的水平”与“本院平均水平”（灰色半透明背景参考线）。
   - **系统管理员视角**：查看某教师时，可切换对比“本院平均”或“全校平均”。
   - 顶部增加“年份切片”下拉框（如 2026, 2025），切换时前端重拉快照数据并加入 View Transition
```


