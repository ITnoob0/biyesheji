------

## 1、

### 🇨🇳 中文提示词 (针对国内 Agent 或中文习惯)

**提示词主题：构建学术社交拓扑图核心模块（MySQL更新 + Neo4j同步 + 图谱API + 前端可视化）**

Markdown

```
# Role: 高级全栈工程师（精通 Django, Neo4j, Vue3, ECharts）

# Context (项目背景)
我们正在开发一个“高校教师科研画像与智能辅助系统”。
当前技术栈：Python 3.11, Django 4.x, Django REST Framework, MySQL 8.0, Neo4j, Vue 3 (Composition API, TypeScript), Element-Plus, Apache ECharts。
我们的目标是开发“学术社交拓扑图”模块。目前的 `achievements` 模块中已经有 `Paper`, `CoAuthor`, `ResearchKeyword` 等 MySQL 模型，现在需要引入 Neo4j 来存储和查询复杂的网络关系，并在前端进行可视化展示。

# Tasks (任务清单)
请按照以下步骤逐步完成代码编写。如果任务过长，你可以分步骤向我确认后再继续。

## 步骤 1：完善 MySQL 数据模型
修改 `backend/achievements/models.py`：
为 `CoAuthor` 模型添加一个外键字段 `internal_teacher`，指向自定义用户模型 `settings.AUTH_USER_MODEL`。允许为空（null=True, blank=True）。这个字段用于将论文中提取的合作者字符串与系统中真实的校内教师账号关联起来。

## 步骤 2：Neo4j 数据同步脚本 (核心引擎)
在 `backend/graph_engine` App 下：
1. 请确保系统中安装官方 `neo4j` Python 驱动包。
2. 编写一个 Django Management Command (例如 `backend/graph_engine/management/commands/sync_neo4j.py`)。
3. 该脚本需要读取 MySQL 中的 `TeacherProfile`, `Paper`, `CoAuthor`, `PaperKeyword` 数据，使用 Cypher 语句将它们同步到 Neo4j 图数据库中。
4. **Neo4j 图谱 Schema 定义要求：**
   - 节点 (Nodes)：`Teacher` (属性: user_id, name), `ExternalScholar` (属性: name), `Paper` (属性: paper_id, title), `Keyword` (属性: name)。
   - 关系 (Edges)：
     - `(Teacher)-[:PUBLISHED {is_first: true/false}]->(Paper)`
     - `(ExternalScholar)-[:CO_AUTHORED]->(Paper)`
     - `(Paper)-[:HAS_KEYWORD]->(Keyword)`

## 步骤 3：开发图谱查询 API (DRF)
在 `backend/graph_engine/views.py` 和 `urls.py` 中：
1. 编写一个 API 接口 (例如 `/api/graph/topology/<int:user_id>/`)，接收一个教师的 User ID。
2. 使用 Cypher 语句查询该教师为中心的 2 度社交网络（合作过的学者、共同关注的关键词等）。
3. **关键约束：** 返回的数据格式必须严格符合 ECharts Graph 系列的要求。
   - 响应格式示例：`{"nodes": [{"id": "1", "name": "张三", "category": 0, "symbolSize": 50}], "links": [{"source": "1", "target": "2", "name": "合作"}]}`

## 步骤 4：前端 Vue3 + ECharts 组件
在 `frontend/src/views/` (或 components) 下创建 `AcademicGraph.vue`：
1. 使用 Vue 3 `<script setup lang="ts">`。
2. 引入 `echarts`。
3. 调用步骤 3 编写的接口获取图谱数据。
4. 渲染一个关系图谱 (series type: 'graph')。
5. **样式要求：** 开启 `force` 力引导布局，节点支持拖拽，不同类型的节点（如中心教师、校内合作者、校外学者、关键词）需要用不同的颜色和 `symbolSize` 区分，并在 `tooltip` 中显示详细信息。

# Constraints (约束)
- 严禁修改现有的 JWT 认证逻辑和跨域配置。
- 所有的 Python 代码必须包含清晰的类型提示 (Type Hints) 和中文注释。
- Neo4j 的连接配置请读取 Django `settings.py` 中的环境变量（给出配置示例）。
- 前端使用 Axios 发送请求，注意携带 Bearer Token。
```

------

### 🇬🇧 英文提示词 (针对 Cursor/Windsurf 等底层为英文原生的 Agent，效果通常更好)

**Prompt Subject: Build Academic Social Network Topology Module (MySQL Update + Neo4j Sync + Graph API + Frontend Viz)**

Markdown

```
# Role: Expert Full-Stack Developer (Django, Neo4j, Vue3, ECharts)

# Context
We are developing a "University Teacher Research Profile & Intelligent Assistant System".
Tech Stack: Python 3.11, Django 4.x, Django REST Framework, MySQL 8.0, Neo4j, Vue 3 (Composition API, TypeScript), Element-Plus, Apache ECharts.
The current goal is to build the "Academic Social Network Topology" module. We already have `Paper`, `CoAuthor`, and `ResearchKeyword` models in MySQL. We need to introduce Neo4j to store and query complex graph relationships and visualize them on the frontend.

# Tasks
Please implement the following steps sequentially. Feel free to ask for confirmation between steps if the output is too long.

## Step 1: Refine MySQL Models
Update `backend/achievements/models.py`:
Add a new foreign key field `internal_teacher` to the `CoAuthor` model. It should point to `settings.AUTH_USER_MODEL`, allowing null and blank values (`null=True, blank=True`). This links raw co-author strings to actual internal user accounts.

## Step 2: Neo4j Data Sync Engine
Inside the `backend/graph_engine` app:
1. Ensure the official `neo4j` Python driver is used.
2. Create a Django Management Command (e.g., `backend/graph_engine/management/commands/sync_neo4j.py`).
3. This script should read data from MySQL (`TeacherProfile`, `Paper`, `CoAuthor`, `PaperKeyword`) and sync it to Neo4j using Cypher queries.
4. **Neo4j Schema Requirements:**
   - Nodes: `Teacher` (props: user_id, name), `ExternalScholar` (props: name), `Paper` (props: paper_id, title), `Keyword` (props: name).
   - Edges:
     - `(Teacher)-[:PUBLISHED {is_first: true/false}]->(Paper)`
     - `(ExternalScholar)-[:CO_AUTHORED]->(Paper)`
     - `(Paper)-[:HAS_KEYWORD]->(Keyword)`
   - *Use MERGE statements to avoid duplicate nodes/edges during repeated syncs.*

## Step 3: Graph Query API (DRF)
In `backend/graph_engine/views.py` and `urls.py`:
1. Create a REST endpoint (e.g., `/api/graph/topology/<int:user_id>/`) that accepts a teacher's user ID.
2. Write a Cypher query to fetch the 2-hop graph network centered around this teacher (including co-authors, papers, and keywords).
3. **Crucial Constraint:** The API response MUST strictly match the format required by Apache ECharts Graph series.
   - Example shape: `{"nodes": [{"id": "1", "name": "Dr. Smith", "category": 0, "symbolSize": 50}], "links": [{"source": "1", "target": "2", "name": "co-author"}]}`

## Step 4: Frontend Vue3 + ECharts Component
Create a new component `AcademicGraph.vue` in `frontend/src/views/` (or components):
1. Use Vue 3 `<script setup lang="ts">`.
2. Import `echarts` and initialize it on an `onMounted` hook.
3. Fetch data from the API created in Step 3.
4. Render an ECharts graph (`series type: 'graph'`).
5. **Styling Requirements:** Use the `force` layout. Make nodes draggable. Differentiate node types (Center Teacher, Internal Co-author, External Scholar, Keyword) using distinct colors and `symbolSize`. Provide a rich `tooltip` for nodes and edges.

# Constraints
- Do NOT modify existing JWT authentication or CORS setups.
- All Python code must include Type Hints and standard docstrings.
- Provide a clear example of how to configure Neo4j credentials in Django `settings.py`.
- On the frontend, ensure Axios requests include the Authorization Bearer token.
```

## 2、

### 🇨🇳 中文提示词（适用于中文大模型 Agent）

**提示词主题：完成画像模块集成（动态路由 + 六维雷达图 API + 画像大屏布局 + 自动化图谱同步）**

Markdown

```
# Role: 高级全栈架构师 (Vue 3, Element-Plus, Django, ECharts)

# Context
我们正在开发“高校教师科研画像与智能辅助系统”。
目前 `AcademicGraph.vue` 组件已经写好，但内部的 `userId` 是写死的，且尚未集成到主页面。我们现在的目标是彻底完成“图谱与画像模块”的全栈开发。
包含：组件动态化、开发六维评价雷达图的后端 API 与前端组件、组装综合画像大屏，以及后端图谱自动化同步。

# Tasks
请按顺序分步执行以下开发任务。每次修改代码请给出完整的相对路径。

## 步骤 1：前端组件动态化与路由配置
1. 修改 `frontend/src/views/AcademicGraph.vue`：移除写死的 `const userId = 1;`，改用 `defineProps<{ userId: number | string }>()` 接收父组件传参。使用 `watch` 或在外部控制渲染，确保 `userId` 变化时能重新拉取图谱数据。
2. 修改 `frontend/src/router/index.ts`：添加或修改一个路由，例如 `/profile/:id`，指向 `DashboardView.vue`，并将路由参数作为 `userId` 提取出来。

## 步骤 2：后端六维雷达图 API 开发
1. 在 `backend/achievements/views.py` 中，新建一个 API 视图 `TeacherRadarView` (路径例如 `/api/achievements/radar/<int:user_id>/`)。
2. 该 API 需要计算该教师的六个维度得分（满分 100 分）。请编写一套基础的打分逻辑（可暂用简单的数学公式，比如基于论文数量、项目数量等计算得出 0-100 的数值）。
   - 六个维度名称："基础学术产出", "经费吸附与攻坚", "硬核知识产权", "人才培养成效", "学术活跃与声誉", "跨学科融合度"。
3. 在 `backend/achievements/urls.py` 中注册该路由。

## 步骤 3：前端雷达图组件与画像大屏组装
1. 在 `frontend/src/components/` 下新建 `RadarChart.vue`：使用 ECharts 渲染雷达图，通过 `defineProps` 接收来自 API 的六维数据，图表需美观（可加渐变色填充）。
2. 彻底重构 `frontend/src/views/DashboardView.vue`：
   - 使用 Element-Plus 的 `el-row` 和 `el-col` 进行页面布局。
   - 顶部：显示教师的姓名、学院、职称等基本信息（可 mock 或调用现有 API）。
   - 主体左侧：放置 `RadarChart.vue`，展示教师的六维综合能力评价。
   - 主体右侧或下方：放置 `AcademicGraph.vue`，展示学者的学术关系拓扑。
   - 逻辑：从 `vue-router` 的 `useRoute` 获取 `id`，传递给上述两个子组件。

## 步骤 4：后端图谱自动同步 (Django Signals)
1. 在 `backend/graph_engine/` 下新建 `signals.py`。
2. 使用 `@receiver(post_save, sender=Paper)` 监听论文的保存动作。
3. 当触发时，自动执行之前在 `sync_neo4j.py` 中的逻辑（或者使用 `subprocess.run` 异步调用该 management command，避免阻塞主线程；若有更优雅的单节点更新 Cypher 也可以）。
4. 确保在 `backend/graph_engine/apps.py` 的 `ready()` 方法中导入该 signal，使其生效。

# Constraints
- 前端必须使用 Vue 3 `<script setup lang="ts">` 语法和 Composition API。
- ECharts 初始化时请注意 DOM 是否挂载完成 (`onMounted` 或 `nextTick`)，并在组件卸载时调用 `chart.dispose()` 防止内存泄漏。
- UI 设计需体现出“数据驾驶舱”的现代感。
```

------

### 🇬🇧 英文提示词（推荐，对代码细节把握更准）

**Prompt Subject: Finalize Profile Module (Dynamic Routing + 6D Radar API + Dashboard Layout + Auto Graph Sync)**

Markdown

```
# Role: Expert Full-Stack Architect (Vue 3, Element-Plus, Django, ECharts)

# Context
We are building a "University Teacher Research Profile & Intelligent Assistant System".
Currently, the `AcademicGraph.vue` component is built but uses a hardcoded `userId = 1` and is not integrated into a main dashboard. Our goal now is to completely finish the "Profile & Graph Module".
This includes: making the graph dynamic, creating a backend API and frontend component for a 6-Dimensional Evaluation Radar Chart, assembling the master Profile Dashboard layout, and implementing backend automated Neo4j syncing.

# Tasks
Please execute the following steps sequentially. Always provide the full relative file path for any code you write.

## Step 1: Frontend Dynamic Components & Routing
1. Update `frontend/src/views/AcademicGraph.vue`: Remove the hardcoded `const userId = 1;`. Instead, use `defineProps<{ userId: number | string }>()` to accept the ID from the parent component. Ensure the data fetch triggers correctly when the component mounts or the prop changes (e.g., using `watch`).
2. Update `frontend/src/router/index.ts`: Add or modify a route (e.g., `/profile/:id`) pointing to `DashboardView.vue`.

## Step 2: Backend 6D Radar Chart API
1. Create a new API view `TeacherRadarView` in `backend/achievements/views.py` (Endpoint e.g., `/api/achievements/radar/<int:user_id>/`).
2. This API must calculate and return scores (0-100) for six dimensions: "基础学术产出" (Academic Output), "经费吸附与攻坚" (Funding/Projects), "硬核知识产权" (IP/Patents), "人才培养成效" (Talent Cultivation), "学术活跃与声誉" (Academic Reputation), "跨学科融合度" (Interdisciplinary Fusion).
3. Implement a basic scoring algorithm (it can be simple math based on object counts like Paper.objects.filter() for now, capped at 100).
4. Register the endpoint in `backend/achievements/urls.py`.

## Step 3: Frontend Radar Component & Dashboard Assembly
1. Create `frontend/src/components/RadarChart.vue`: Initialize an ECharts radar chart. Use `defineProps` to accept the 6D data from the parent. Style it beautifully (e.g., with area fill opacity).
2. Completely refactor `frontend/src/views/DashboardView.vue`:
   - Use Element-Plus layout components (`el-row`, `el-col`, `el-card`).
   - Header/Top: Display basic teacher info (Name, Department, Title). You can mock this data temporarily or fetch it if an API exists.
   - Body Left: Render the `RadarChart` component.
   - Body Right/Bottom: Render the `AcademicGraph` component.
   - Logic: Extract the `id` parameter from `useRoute()` and pass it down as `userId` to the child graph and radar components.

## Step 4: Backend Automated Graph Sync (Django Signals)
1. Create `backend/graph_engine/signals.py`.
2. Implement a `post_save` signal receiver for the `Paper` model (`@receiver(post_save, sender=Paper)`).
3. When a Paper is saved, execute a process to update the Neo4j graph. (For simplicity and non-blocking behavior, you can trigger a Celery task if configured, or use Python's `threading`/`subprocess` to run the existing `sync_neo4j` command asynchronously).
4. Import the signal in the `ready()` method of `backend/graph_engine/apps.py` to ensure it registers.

# Constraints
- Strict use of Vue 3 `<script setup lang="ts">` and Composition API.
- For ECharts, ensure the DOM is ready before initialization and call `dispose()` `onBeforeUnmount` to prevent memory leaks.
- Ensure all API requests include the JWT Authorization header.
- Python code must include Type Hints and proper error handling.
```

## 3、

### 🇨🇳 中文提示词 (Chinese Prompt)

**建议将以下内容直接复制给你的 AI 编程助手：**

Plaintext

```
# 角色
你是一个经验丰富的全栈开发专家，精通 Vue 3 (Composition API, TypeScript), Element Plus 和 Django REST Framework。

# 任务目标
请为当前的科研管理系统开发“成果录入中心 (Achievement Entry Center)”功能。目前后端的 `PaperViewSet` 已经存在并支持 POST 请求录入数据，且包含了 AI 关键词提取和 Neo4j 节点同步逻辑。你的主要任务是开发前端的 Vue 3 录入页面，并对接后端的 `/api/achievements/papers/` 接口。

# 技术栈限制
- 前端：Vue 3 (`<script setup lang="ts">`), Element Plus, Pinia, Axios, Vite。
- 样式：尽量使用 Element Plus 提供的原生组件和 CSS 变量，保持 UI 风格一致。

# 详细执行步骤

1. **创建视图组件**:
   - 在 `frontend/src/views/` 目录下创建新文件 `AchievementEntryView.vue`。
   - 页面整体应包含一个 Element Plus 的卡片 (`el-card`)，标题为“科研成果录入”。

2. **构建动态表单 (Element Plus)**:
   - 使用 `el-form`, `el-form-item`, `el-input`, `el-date-picker` 构建表单。
   - **必填字段**：
     - 论文标题 (`title`): 单行文本框。
     - 摘要 (`abstract`): 多行文本框 (type="textarea"，建议至少 4 行)，提示用户输入较长文本以便后端 AI 进行关键词提取。
     - 获取/发表日期 (`date_acquired`): 日期选择器 (value-format="YYYY-MM-DD")。
   - **选填字段**：
     - DOI (`doi`): 单行文本框。
     - 外部合作者 (`coauthors`): 目前可暂留为一个输入框或标签输入组件（提示前端传递适当的数据格式对接后端）。

3. **表单验证 (Form Validation)**:
   - 定义 `rules` 对象，确保 `title`, `abstract`, `date_acquired` 均有 `required: true` 验证规则和相应的触发事件 (blur/change)。

4. **API 对接与状态处理**:
   - 引入 `axios` 或项目封装的 HTTP 客户端。
   - 编写 `submitForm` 异步函数：
     - 提交前先调用表单的 `validate` 方法。
     - 验证通过后，发送 `POST` 请求到后端的 `/api/achievements/papers/` 路由（假设基础路由前缀已配置）。
     - 显示加载状态 (`loading` 变量绑定到提交按钮的 `loading` 属性)，防止重复提交。
     - 提交成功后：使用 `ElMessage.success` 提示用户“录入成功，后台正在进行 AI 分析和图谱构建”，并调用表单的 `resetFields()` 清空表单。
     - 提交失败后：捕获错误并使用 `ElMessage.error` 显示具体的错误信息。

5. **配置路由**:
   - 修改 `frontend/src/router/index.ts`，将新创建的 `AchievementEntryView.vue` 添加到路由表中，路径可以设为 `/entry`。

请直接输出完整的前端 Vue 3 代码和路由修改代码。
```

------

### 🇬🇧 英文提示词 (English Prompt)

**If your AI agent performs better with English context, use this version:**

Plaintext

```
# Role
You are an expert full-stack developer highly proficient in Vue 3 (Composition API, TypeScript), Element Plus, and Django REST Framework.

# Objective
Please develop the "Achievement Entry Center" for the current academic management system. The backend `PaperViewSet` already exists and handles POST requests, including integrated logic for AI keyword extraction and Neo4j graph synchronization. Your primary task is to build the frontend Vue 3 entry form and integrate it with the backend `/api/achievements/papers/` endpoint.

# Tech Stack Constraints
- Frontend: Vue 3 (`<script setup lang="ts">`), Element Plus, Pinia, Axios, Vite.
- Styling: Utilize Element Plus native components and CSS variables to maintain consistent UI styling.

# Detailed Execution Steps

1. **Create the View Component**:
   - Create a new file named `AchievementEntryView.vue` in the `frontend/src/views/` directory.
   - The main layout should use an `el-card` component with the header "Achievement Entry".

2. **Build the Form (Element Plus)**:
   - Use `el-form`, `el-form-item`, `el-input`, and `el-date-picker` to build the form layout.
   - **Required Fields**:
     - Paper Title (`title`): Standard text input.
     - Abstract (`abstract`): Textarea (min 4 rows). Add a placeholder hinting that detailed abstracts help the backend AI extract accurate keywords.
     - Date Acquired (`date_acquired`): Date picker (value-format="YYYY-MM-DD").
   - **Optional Fields**:
     - DOI (`doi`): Standard text input.
     - Co-authors (`coauthors`): Leave as a tag input or basic text field for now, preparing the payload format based on backend requirements.

3. **Form Validation**:
   - Define a `rules` reactive object to ensure `title`, `abstract`, and `date_acquired` are validated as `required: true` with appropriate trigger events (blur/change).

4. **API Integration & State Management**:
   - Import `axios` (or the project's customized HTTP client).
   - Implement an asynchronous `submitForm` function:
     - Call the form's `validate` method before submission.
     - Upon successful validation, send a `POST` request to `/api/achievements/papers/`.
     - Implement a `loading` state bound to the submit button to prevent double submissions.
     - On Success: Trigger an `ElMessage.success` notifying the user "Submission successful. AI analysis and graph construction in progress in the background." Then, call `resetFields()` to clear the form.
     - On Error: Catch exceptions and display specific error details using `ElMessage.error`.

5. **Configure Routing**:
   - Update `frontend/src/router/index.ts` to include the newly created `AchievementEntryView.vue`, mapping it to the path `/entry`.

Please output the complete Vue 3 component code and the necessary router modifications.
```

### 🇨🇳 中文提示词 (Chinese Prompt)

**建议将以下内容直接复制给你的 AI 编程助手：**

Plaintext

```
# 角色
你是一个经验丰富的 Vue 3 和前端工程化专家。

# 背景
我已经在 Vue 3 + Vite 项目中创建了“成果录入”页面 (`AchievementEntryView.vue`)，并在 `src/router/index.ts` 中注册了 `/entry` 路由。为了让该功能完整可用，我需要你帮我完成后续的全局配置工作。

# 任务列表

请帮我修改或创建以下配置：

1. **添加系统导航入口 (Navigation)**:
   - 请检查项目的主布局文件（例如 `src/App.vue` 或 `src/views/DashboardView.vue` 中的菜单/侧边栏部分）。
   - 添加一个指向 `/entry` 的导航链接或 Element Plus 菜单项（如 `<el-menu-item index="/entry">成果录入</el-menu-item>`）。

2. **配置全局 Axios 拦截器 (Token Injection)**:
   - 我们的系统通过 `localStorage.getItem('token')` 来管理登录状态。
   - 请在 `src/main.ts` 中（或者创建一个单独的 `src/utils/axios.ts` 并在 main.ts 中引入），配置全局的 Axios 请求拦截器 (Request Interceptor)。
   - 逻辑：在每次发送请求前，检查 localStorage 中是否有 token，如果有，则在请求头中携带它：`config.headers.Authorization = 'Bearer ' + token`（请根据常规 JWT 或 Token 认证格式调整，如果是 Django Token 认证可能是 `'Token ' + token`）。

3. **配置 Vite 跨域代理 (CORS Proxy)**:
   - 前端代码中直接请求了 `/api/achievements/papers/`。由于前后端分离，需要配置代理。
   - 请修改项目根目录的 `vite.config.ts` 文件。
   - 在 `server` 配置块中添加 `proxy` 规则：将所有以 `/api` 开头的请求代理到本地的 Django 后端地址（假设后端运行在 `http://127.0.0.1:8000`），并确保保留原始的 `/api` 路径或根据需要进行 rewrite。

请直接输出需要修改的文件的完整代码或提供精确的 Diff 代码块。
```

------

### 🇬🇧 英文提示词 (English Prompt)

**如果你的 AI 助手在英文语境下表现更好，请使用这个版本：**

Plaintext

```
# Role
You are an expert in Vue 3 and frontend engineering.

# Context
I have already created the "Achievement Entry" page (`AchievementEntryView.vue`) in my Vue 3 + Vite project and registered the `/entry` route in `src/router/index.ts`. To make this feature fully functional, I need your help to complete the remaining global configurations.

# Task List

Please modify or create the following configurations:

1. **Add System Navigation Entry**:
   - Check the main layout file of the project (e.g., `src/App.vue` or the menu/sidebar section in `src/views/DashboardView.vue`).
   - Add a navigation link or an Element Plus menu item pointing to `/entry` (e.g., `<el-menu-item index="/entry">Achievement Entry</el-menu-item>`).

2. **Configure Global Axios Interceptor (Token Injection)**:
   - Our system manages the login state using `localStorage.getItem('token')`.
   - Please configure a global Axios Request Interceptor in `src/main.ts` (or create a separate `src/utils/axios.ts` and import it into main.ts).
   - Logic: Before every request, check if a token exists in localStorage. If it does, attach it to the request headers: `config.headers.Authorization = 'Bearer ' + token` (adjust to `'Token ' + token` if it's Django's standard Token Auth).

3. **Configure Vite CORS Proxy**:
   - The frontend requests `/api/achievements/papers/` directly. Since the frontend and backend run on different ports, a proxy is needed to avoid CORS issues.
   - Please modify the `vite.config.ts` file in the project root.
   - Add a `proxy` rule inside the `server` block: proxy all requests starting with `/api` to the local Django backend (assuming it runs on `http://127.0.0.1:8000`). Ensure the original `/api` path is preserved or rewritten properly based on standard proxy configuration.

Please output the complete code for the modified files or provide precise Diff blocks.
```