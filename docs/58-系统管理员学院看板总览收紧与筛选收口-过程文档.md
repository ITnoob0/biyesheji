# 58-系统管理员学院看板总览收紧与筛选收口-过程文档

## 目标

收紧系统管理员端学院看板总览，使筛选与总览结构更清晰。

## 本次调整

- 删除 `按合作情况筛选`
- 删除 `趋势对比` 导出项
- 将 `重置筛选`、`导出当前数据` 挪到右上角
- 删除 `合作活跃度概览`
- 总览改为：
  - `年度成果趋势` 单独占一整行
  - `范围结构分布` 与 `全校管理摘要` 同行展示

## 验证

- 前端类型检查通过
